# Handoff: RIT validator review — VNG-EYE app

**Project root:** `c:\Users\sudhi\OneDrive\Documents\Neurology Talks\VNG-EYE app`
**Branch:** `feature/cft-implementation`
**Status:** paused mid-session, nothing committed
**Date of snapshot:** 2026-06-29

## What this project is

VNG-EYE app — video nystagmography eye-tracking tool. Current focus is the **RIT (Rescue Iris Tracker)** — an image-content validator that gates iris candidates the geometry-only V1 limbus tracker accepts. Goal: stop the tracker locking onto skin / brow / shadow when the real iris is lost, without redesigning V1.

## Locked vocabulary (do not paraphrase)

RIT / RIT Stage 0 / RIT Tracking / RIT Guardrails / RIT Rescue. Never "V1-plus-anatomy".

## Hard rules from this session (carry forward)

- Do **not** redesign the tracker.
- Do **not** change the RIT validator concept or thresholds yet.
- Do **not** implement moving-orbit tracking yet.
- Do **not** implement nystagmus detection.
- Do **not** commit unless explicitly asked. User controls commit boundaries.

## Working tree (uncommitted)

```
 M EYE_VNG_DEVELOPMENT_HISTORY.md      (RIT naming lock block, 2026-06-29)
 M iris_tracker.py                     (RIT wiring + --disable-rit-validator/--output-suffix flags)
?? Compare_RIT_Guardrails_Vestibular_Neuritis.bat   (launcher)
?? src/core/iris_candidate_validator.py             (the V2 image-content validator)
?? tools/                                            (contains rit_compare_scrubber.py)
```

## RIT validator — current shape

- New module `src/core/iris_candidate_validator.py` — image-content gate the geometry-only acceptance was missing. Conservative by design: false-negative (freeze) acceptable, false-positive (lock on skin/brow/shadow) dangerous.
- Wired into `iris_tracker.py::_apply_v1_plausibility`. V1 limbus engine untouched — validator only *judges* candidates.
- Three wiring fixes applied this session:
  - dead `_v1_plausibility_check` removed
  - stale-oval `drift_reason` now appends `|iris_validator:outside_orbit`
  - hard-fail `no_stage0_radius` guard added before calling the validator
- Optional `if gray is None` guard added inside the validator.

## Test clip & current validator stats

Clip: `samples\nystagmus at rest to left in right vestibular neuritis.mp4` (640 frames).

Per-eye reject stats from `tracking.csv`:
- **Left:** iris_valid_true = 6 / 640. Top rejects: `outside_orbit` 174, `limbus_unfit` 105 (V1-internal), `no_sclera_support` 22.
- **Right:** iris_valid_true = 32 / 640. Top rejects: `outside_orbit` 155, plus 28× `orbit_status:good_iris_but_static_orbit_stale|iris_validator:outside_orbit` (Fix 2 confirmed working).
- Stage-0 limbus radii ≈ 82 px L / 81 px R. No `radius_implausible` rejects survived to the validator (V1 already killed small-radius outliers).

## Comparison scrubber (new review tool, ready to use)

- Script: `tools\rit_compare_scrubber.py`. Pure review tool — no tracker logic runs in it. Reads two CSVs side-by-side.
- **LEFT panel** = no-guardrails baseline; **RIGHT panel** = guarded (current clinical) output. Same frame number.
- Output folders kept separate; guarded folder is **never** overwritten:
  - guarded:  `outputs\nystagmus at rest to left in right vestibular neuritis_tracked\`
  - baseline: `outputs\nystagmus at rest to left in right vestibular neuritis_tracked_no_guardrails\` (generated with `--disable-rit-validator --output-suffix "_no_guardrails"`)
- Saved comparison PNGs go to `<guarded>\_rit_compare_scrubber\` plus `saved_frames.csv`.
- Launch: double-click `Compare_RIT_Guardrails_Vestibular_Neuritis.bat`, or `py -3.14 tools\rit_compare_scrubber.py "<video>" --eye BOTH`.
- Controls: arrows/AD step, PgUp/PgDn ±10, Home/End, N/B jump rejected, G goto, S save, Q/Esc quit.

## The open clinical question

At frame 50, the LEFT panel shows the raw V1 candidate cleanly on each iris; the RIGHT panel shows both candidates struck through with `REJECTED: outside_orbit`. The dominant reject mode on this clip is either:

- **A — real face drift** (validator correctly rejecting), or
- **B — good iris, stale static orbit** (validator rejecting good candidates because the orbit box hasn't moved with the face).

Smoke test hints at **B**, meaning the eventual fix is moving-orbit tracking — **not** loosening RIT thresholds. Confirm by scrubbing the side-by-side:
- frames flickering **red** = real concern (A)
- frames flickering **amber** = stale-static-orbit pile (B)

## Python environment

System `python` (3.12) lacks cv2 on this machine. Use **`py -3.14`** for anything that imports cv2 or runs the tracker. The launcher .bat already uses `py -3.14`. The `.venv\Scripts\python.exe` shim is broken (points at a different user's Python path) — ignore it.

## Recent commits on branch (context)

```
32aca95 Save VNG eye tracking reset and V2 iris rules
3225bcf Define arrow-based nystagmus detection as V1 primary output
b13c4e0 Implement V1 limbus-contour tracker with best-eye anatomical selection
453ca58 Update V1 architecture to limbus tracker with eye-opening contour reference
8a5973a Add EyeVNG V1 design history and architectural evolution
```

## Resume checklist for Claude Desktop

Before suggesting any work, verify the snapshot still matches reality:
1. `git status --short` — confirm the M/?? files above are still uncommitted.
2. Confirm both output folders still exist (`_tracked\` and `_tracked_no_guardrails\`).
3. Confirm `tracking.csv` reject counts still match (validator behaviour unchanged).
4. Then proceed to side-by-side scrubbing to classify rejects as A vs B.

## Immediate next step

Scrub the side-by-side comparison and classify rejects as **A (real drift)** vs **B (stale static orbit)**:

1. Double-click `Compare_RIT_Guardrails_Vestibular_Neuritis.bat`.
2. Step through frames — use **N** to jump to the next rejected frame.
3. Tally:
   - **Red flicker** (LEFT panel candidate also looks wrong) = **A**, validator correctly rejecting real drift.
   - **Amber flicker** (LEFT panel shows clean iris, RIGHT rejects as `outside_orbit`) = **B**, orbit box is stale.
4. Save representative frames with **S** as evidence for each class.

Expected outcome (hypothesis, not assumption): **B dominates**, meaning the right fix is moving-orbit tracking, not loosening RIT thresholds.

---

*Older session handoff preserved in [HANDOFF.md](HANDOFF.md).*
